import React from "react";
import { Grid, Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Grid container item sx={{ mt: 7 }} justifyContent="center">
      <Typography variant="h3">Not found page</Typography>
    </Grid>
  );
};

export default NotFound;
